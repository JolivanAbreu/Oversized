VITE_API_BASE_URL=http://localhost:3000/v1
VITE_MERCADOPAGO_PUBLIC_KEY=

# URL do painel administrativo — usado pelo botão "Painel" que aparece no
# cabeçalho quando um admin/operador está logado na loja. Ajuste para a URL
# real do painel em produção (ex.: https://admin.dravennx.com.br).
VITE_ADMIN_PANEL_URL=http://localhost:5174
